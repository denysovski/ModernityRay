/* Shared UI primitives. Import from '../ui' — never reach into the files
   directly, so pieces can move without touching every call site. */
export { Icon } from './Icon'
export { Reveal, Stagger, Pop, Depth, upItem, stagger, popItem } from './motion'
export { default as CountUp } from './CountUp'
